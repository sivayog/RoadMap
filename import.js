// import {  User, printName } from './User.js'; // Use import instead of require

// const userName = new User('siva', 'ananthi');
// printName(userName);
//dynmaic import method
// import ('./User.js').then(({  User, printName })=>{
   
// const userName = new User('siva', 'ananthi');
// printName(userName);
// })

// setTimeout(() => {
//     import ('./User.js').then(({  User, printName })=>{
   
//         const userName = new User('siva', 'ananthi');
//         printName(userName);
//         })
// }, 5000);

//condtional importtimg staetment 

// import (`./${User.loacale}-transulation.js`)

// above is the one of the example file suppose 
// we have multiple file that time if we want to  
// import one file at a time above template 
// string like dynmaic import we can use 